Quick setup.

cd into each day then work with `cargo run --bin part1` or `part2` etc. make new days via `cargo new day-<01-12>`

Rust anaylizer does not like the subfolders. Either update `.vscode/settings` or just open the day folders.
